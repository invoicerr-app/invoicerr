# TODO — reconstruire sur le modèle de document générique

> Inventaire de ce que la suppression du système de documents et du moteur de conformité
> (commit `fffbae77`, repère git `avant-refonte-documents`) a emporté et qu'il faut remettre —
> cette fois sur le nouveau modèle : un type de document est un descripteur, un pays est une
> donnée, tout ce qui est déclaré sans implémentation bloque en le disant.
>
> Établi le 2026-08-30 en comparant les modules du repère avec ceux d'aujourd'hui.
> Remplace `TODO_MASTER_ROADMAP.md`, écrit pour l'architecture d'avant la démolition et
> conservé comme référence historique.
>
> **⚖ = demande de sourcer du droit.** C'est le vrai coût de ces items, pas le code : chaque
> affirmation juridique porte sa source et sa date de consultation, ou est marquée `unverified`
> avec ce qui la trancherait. Ne jamais inventer une règle fiscale ou juridique.

## Dépendances utiles pour ordonner

- **1 → 4, 12, 24** (pas de pièce jointe, de format normalisé ni de lien public sans rendu PDF)
- **17 → 16** (l'arithmétique domestique avant la composition transfrontalière)
- **7 → 8** (pas de lettrage sans paiements)
- **3** conditionne la finesse de tout le reste (la politique pays par statut)
- **10** rouvre la voie aux preuves live conservées au repère (PDP, KSeF)

---

## A — Le quotidien des documents

| # | Titre | Description |
|---|---|---|
| 1 ✅ | **Rendu PDF** (fait, 2026-08-30) | Aucun moteur de rendu n'existe (l'aperçu affiche les données brutes en le disant). Un moteur **générique piloté par le descripteur** — un template par kind de champ, pas par type — pour que tout type de document ait son PDF sans écran dédié. Préalable aux items 4, 12 et 24. |
| 2 ✅ | **Numérotation** ⚖ (fait, 2026-08-31) | Les documents n'ont plus aucun numéro. Compteurs par société et par type, format configurable, numéro pris à l'émission. La règle « séquence sans trou » est du droit à sourcer par pays — l'ancien code (et son bug du `-0000`) est au repère pour référence. |
| 3 ✅ | **Statuts & cycle de vie** (fait, 2026-08-31) | Aujourd'hui : `draft`/`sent` et guère plus. Déclarer les statuts et transitions **par type, en donnée**, et donner à la politique pays la granularité par statut qui lui manque (« corriger, mais seulement une fois parti »). |
| 4 ✅ | **Envoi avec pièce jointe** (fait, 2026-08-31) | L'envoi joint désormais le PDF (le même moteur que l'item 1, jamais une seconde implémentation) — `<displayNumber>.pdf` une fois numéroté, `<typeId>-<id>.pdf` sinon. Gabarits de mail par type de document (`DocumentTypeDescriptor.email`), surchargeables par société (`Company.documentEmailTemplates`) — la table `MailTemplate` pré-existante n'a pas été réutilisée (enum fermé, vocabulaire différent, aucun type "quote" ; voir `actions/company-email-templates.ts`). Un échec de rendu PDF fait échouer l'envoi (jamais un courriel sans pièce jointe envoyé en silence) ; un placeholder de gabarit inconnu est laissé tel quel avec un avertissement, jamais une exception. Prouvé en vrai contre Mailpit (`send-quote.live.spec.ts`, PDF + sujet interpolé réellement reçus). |
| 5 ✅ | **Factures récurrentes** (fait, 2026-08-31) | Mécanisme générique (`documents/schedules/`) : un `DocumentSchedule` rejoue une ACTION sur un document source à une cadence fermée (`weekly`/`monthly`/`quarterly`/`yearly`, jamais de RRULE). Un seul job répétable (le balayage, sur la file existante) tire une occurrence par schedule dû, jobId déterministe incluant l'occurrence pour la déduplication. Le cas facture : `duplicate` avec dates recalées (délai source préservé), enchaînement `send` optionnel appelé SYNCHRONE (hors du job, sinon collision avec sa propre ré-inscription — voir `schedule-sweep-runner.ts`). Écran : action de ligne « Recurrence » générique (gate sur l'action `duplicate`), onglet Réglages « Recurrences ». Un échec d'occurrence pose `lastError`, visible, sans désactivation silencieuse. |
| 6 ✅ | **Remises & acomptes** (fait, 2026-08-31, avec le pré-remplissage catalogue) | L'ancien système avait la remise par ligne, la facture d'acompte et la conversion devis→facture finale. À redéclarer dans les descripteurs. |

## B — L'argent

| # | Titre | Description |
|---|---|---|
| 7 ✅ | **Paiements** (fait, 2026-08-31) | `record-payment` est déclarée sur la facture et bloque en 501 — c'est le cas vivant du test de blocage. Modèle de paiement, écran, méthodes de paiement (module supprimé). |
| 8 ✅ | **Lettrage** (fait, 2026-08-31) | Rapprocher paiements ET avoirs de la facture qu'ils soldent — le `settlementOf` d'avant était pur et vérifié par mutation, récupérable au repère. Dépend de 7. |
| 9 ✅ | **Multi-devises** (fait, 2026-08-31) | Devise de référence opt-in (`Company.referenceCurrency`, null = rien ne change), taux saisis à la main (`CurrencyRate`, jamais d'inverse dérivé), consolidation dashboard qui porte toujours son taux — ou rien du tout si un taux manque. Une conversion est une information, jamais un remplacement : les montants d'origine restent la vérité. L'ancienne `CurrencyConversion` (globale, Float, TTL) rejetée en bloc. Le lettrage ne convertit toujours pas — choix consigné dans `TODO_ISSUES.md`. |

## C — La sortie (conformité)

| # | Titre | Description |
|---|---|---|
| 10 ✅ | **Transports nationaux** (vague 1 : socle credentials + PDP, 2026-08-31 ; vague 2 : KSeF + SdI, 2026-08-31 — item clos) | Le registre n'a que `email`. Réintégrer PDP, KSeF, SdI… comme transports enregistrés avec credentials par société. Vague 1 : `CompanyChannelConfig` (AES-256-GCM, `modules/company/channels/`), transport `pdp` (`documents/transports/pdp-transport.ts`) déposant un Factur-X (`formats/facturx-provider.ts`, item 12), écran « Canaux » (`/settings/channels`, suggestion FR→PDP en donnée pays, jamais un `if`). Dépôt réel PROUVÉ (superpdp sandbox, `pdp/pdp.live.spec.ts`) — l'accusé de dépôt seulement, le suivi du statut de conformité (poll) reste à faire, voir TODO_ISSUES.md. Vague 2 : deux formats NATIONAUX (`formats/national/fa3-provider.ts` PL, `fatturapa-provider.ts` IT), CHACUN jugé par son propre XSD OFFICIEL vendoré octet pour octet (`formats/vendored/{pl,it}/`, `vendored/validate-xsd.ts` — xmllint-wasm, jamais un compilateur maison, jamais le Schematron EN 16931) ; deux transports `ksef`/`sdi` (`transports/ksef-transport.ts`/`sdi-transport.ts`) — KSeF reprend VERBATIM le client + la crypto prouvés au repère (`transports/ksef/ksef-client.ts`/`ksef-crypto.ts`, mêmes tests) ; SdI reprend le client mais AUCUNE implémentation SOAP réelle n'existe (`sdi/sdi-client.ts`'s own `UNACCREDITED_SDI_HTTP_PORT`, accréditation AdE non obtenue — `send()` échoue honnêtement, toujours, en le disant). Écran « Canaux » généralisé (les champs de config sont déclarés par provider, `PROVIDER_FIELDS`, plus de branche PDP en dur) ; suggestions PL→KSeF et IT→SdI en donnée pays (item 11 tranchera le caractère obligatoire, pas celui-ci). Aucun des deux canaux n'a de POLL vers la clearance/notifica — voir TODO_ISSUES.md, même gap que PDP. |
| 11 ✅ | **Canal imposé par pays** ⚖ (fait, 2026-08-31) | `channel-suggestion/` devenu `transports/channel-policy/` : une entrée est `{providerId, requirement: suggested\|mandated, mandatedFrom?, provenance}`. Un `mandated` sans provenance `legal` (citation + date de consultation) **échoue au chargement** — une règle sans source ne charge pas. Le mandat s'évalue à la date d'ÉMISSION (jamais l'horloge serveur — stable sous l'envoi asynchrone rejoué) ; l'envoi par un autre canal, ou par le canal imposé non connecté, est refusé au preflight en nommant le canal, la source et le remède. Seule la France est `mandated` (PDP dès 2026-09-01, citation impots.gouv.fr reprise verbatim du repère, consultée 2026-08-27) ; PL/KSeF et IT/SdI restent `suggested` — leurs profils au repère encodaient les dates sans citation propre, les promouvoir aurait été de la foi, pas du droit. Badge « Mandatory from » + bannière à l'écran. |
| 12 ◐ | **Formats normalisés** ⚖ (CII/UBL/Factur-X faits, 2026-08-31 ; reste XRechnung/Peppol BIS) | EN 16931 CII, UBL 2.1 et Factur-X (PDF/A-3 + CII embarqué, item 10) : `documents/formats/` (registre de `DocumentFormatProvider`, motif `transports/transport-registry.ts`), pont descripteur → modèle sémantique (`semantic/build-semantic-invoice.ts`, montants venant de `compute-totals.ts`, jamais recalculés), gate XSD-équivalent + Schematron EN 16931 VENDORÉ (repris du repère, jamais un compilateur maison) — un artefact invalide n'est jamais servi. Action `download-xml` sur la facture (param `syntax` ∈ {cii, ubl, facturx}), bouton front, i18n. XRechnung/Peppol BIS (rulesets rangés, non branchés) et la légalité du caractère obligatoire (item 11/15) sont consignés dans `TODO_ISSUES.md`, pas devinés ici. Dépend de 1 et 17. |
| 13 ✅ | **Signature électronique** (fait, 2026-08-31) | Reprise du repère : providers XAdES/CAdES/PAdES réels + TSA RFC 3161 (`documents/signing/`), `CompanySigningCertificate` (PFX + mot de passe chiffrés AES-256-GCM, métadonnées extraites à l'upload, expiré refusé à l'upload ET au resolve, JAMAIS renvoyés ni loggés). La signature est une CAPACITÉ opt-in de société — aucun pays du produit n'en exige une aujourd'hui, aucune obligation inventée. Câblé : le PDF servi et joint passe en PAdES-BES quand un certificat actif applicable existe ; sans certificat, octets strictement inchangés (prouvé par les specs préexistantes intactes). Amélioration sur le repère : une crypto qui échoue avec un cert actif LÈVE (le repère avalait en warn). XAdES/CAdES repris et testés mais non câblés à un flux (aucun n'en a besoin) ; LT/LTA traités T (couture documentée). TSA prouvé LIVE contre FreeTSA (jeton RFC 3161 réel, rejoué en validation). Écran `/settings/signing`, spec 33. |
| 14 ✅ | **Archivage légal** ⚖ (fait, 2026-08-31) | `documents/archive/` : au `sent` de l'envoi asynchrone (`actions/async-send.ts`, phase 2), archive LES ARTEFACTS RÉELLEMENT LIVRÉS — le PDF (signé s'il l'était) pour "email", le format structuré déposé (Factur-X/FA(3)/FatturaPA) pour pdp/ksef/sdi, jamais les deux inventés pour un transport qui n'en livre qu'un. Hachage SHA-256 ENCADRÉ (`role|mime|byteLength\n` par artefact, repris du repère `avant-refonte-documents:compliance/providers/archive/storage.ts` — une concaténation nue ferait collider deux jeux d'artefacts différents), persistance locale `DOCUMENTS_ARCHIVE_DIR` (défaut `<cwd>/.documents-archive`, relu à chaque appel, content-hash-addressed). `DocumentArchive` : aucune route de modification/suppression (l'immuabilité est le point). Rétention FR (⚖) : les DEUX durées sourcées du repère s'appliquent SIMULTANÉMENT (fiscale 6 ans LPF art. L102 B ; commerciale 10 ans C. com. art. L123-22) — `retentionUntil` = archivedAt + leur MAXIMUM (10 ans), `retentionBasis` citant les deux ; un pays sans donnée archive quand même, `retentionUntil` NUL et `retentionBasis` disant qu'aucune règle n'est déclarée, jamais une durée inventée (`archive/retention/`, même moule que `mentions/`). Échec d'archivage : l'envoi déjà livré n'est jamais annulé — `DocumentInstance.lastArchiveError` (champ dédié, jamais celui des actions) + log error. `GET /documents/:id/archives` et `POST .../archives/:archiveId/verify` (RE-HACHE les octets stockés, nomme l'artefact et l'attendu/obtenu en cas de CORROMPU) ; écran document (hash abrégé, date, bouton verify), spec 34. WORM/S3 régional NON fait (aucun credential — consigné dans `TODO_ISSUES.md`, honnêteté reprise du repère). |
| 15 ✅ | **Mentions obligatoires** ⚖ (fait, 2026-08-31) | `documents/mentions/` : reprise quasi verbatim du résolveur `invoice-notes.ts` du repère (données pays chargées en fichier — `data/fr.json`, jamais seedées en base, motif `transports/channel-policy/`), avec ses 3 mentions FR (PMT/PMD/AAB, C. com. art. L441-9 I al. 5), leur `legalRef` (une mention sans legalRef ne charge pas) et le taux supplétif temporel (BCE MRO +10 pts, 12,15 % → 12,40 % au 1er juillet 2026), **figé à la date d'émission, jamais recalculé**. Émises en BG-1 du CII/UBL (`build-semantic-invoice.ts`) et en pied de PDF (`render-html.ts`, bloc `.legal-mentions` séparé des notes utilisateur). Bug réel trouvé et corrigé EN COURS DE ROUTE : l'embarquage Factur-X régénère son propre CII en interne, hors d'atteinte du correctif texte `splitCiiIncludedNotes` — corrigé par `splitCiiIncludedNotesInObject`, câblé via le point d'extension `postProcessor` de `@e-invoice-eu/core` (voir `TODO_ISSUES.md`). **Preuve live** (`pdp/pdp.live.spec.ts`) : le rejet `fr:213` du bac à sable superpdp ne cite plus AUCUNE des trois mentions — seul BT-23 (cadre de facturation, hors périmètre, item 12) subsiste. Personnalisation société (taux stipulé, vrai escompte) NON faite — le statutaire d'office était le cœur de l'item ; consignée dans `TODO_ISSUES.md`. |
| 16 ◐ | **Transfrontalier** ⚖ (fait, 2026-08-31 ; OSS hors FR et catégorie E domestique restent hors d'atteinte) | Reprise du moteur pur du repère (`documents/tax/tax-engine.ts` + `classification.ts`, la matrice de 10 cas, `vat-syntax.ts` pour le syntaxique VAT par pays) et câblage à l'entrée en sending (`documents/tax/resolve-invoice-tax.ts`) : domestique inchangé (rate confirmé appartenir au pays, sinon refusé) ; transfrontalier B2B intra-UE validé → autoliquidation (AE, art. 196) ou livraison intra-UE (K, art. 138) ; export hors UE → G (art. 146) ou O ; pays acheteur irrésolu → BLOCAGE DUR nommé (le bug 0% silencieux payé, jamais réinstallé) ; OSS B2C sans table de taux destination → BLOCAGE nommé, jamais un taux inventé ou celui du vendeur appliqué en silence. VAT du client : `country-identifiers/data/de.json` (nouveau) donne un champ VAT à l'écran client pour un pays sans fichier avant cette tâche ; `PartyIdentifier.validationStatus` (déjà au schéma, jamais câblé depuis la démolition) reprend vie — `clients.service.ts`/`clients.module.ts` valident réellement (syntaxique d'abord, VIES ensuite, `NODE_ENV=test` → null client) — prouvé live (`VIES_LIVE=1`, IE6388047V → VALID en direct). BT-151 porte AE/K/G/O réels, jugés par le vrai Schematron vendoré (`documents/tax/cross-border-formats.spec.ts`) ; mentions du moteur (art. 196/138/146) rejoignent BG-1 via le mécanisme `mentions/` existant. Restant nommé dans `TODO_ISSUES.md` : catégorie E domestique toujours hors d'atteinte (le domestique ne traverse pas le moteur, par choix — "rien ne change"), tables de taux OSS sourcées pour FR seulement, mélange de catégories au même taux 0% sur une même facture (limite de `compute-totals.ts`, jamais rencontrée par les scénarios réels ici). |
| 17 ✅ | **Calcul fiscal** (fait, 2026-08-30) | Totaux HT/TVA/TTC à partir du taux choisi par ligne. L'arithmétique domestique d'abord ; base du 16. |

## D — L'entrée

| # | Titre | Description |
|---|---|---|
| 18 ✅ | **Réception de factures** (fait, 2026-09-01) | Nouveau type `received-invoice` (`descriptors/received-invoice.descriptor.ts`) : fournisseur (texte libre, PAS une référence client), n° du fournisseur, dates, devise, montants HT/TVA/TTC à plat (pas de lignes détaillées — consigné), et la référence du fichier original portée dans `DocumentInstance.data` (`fileRef`/`fileName`/`fileMime`, jamais des `DocumentFieldDescriptor` — aucune migration). Statuts `received → approved \| rejected`, `delete` restreint à `received` seul (une décision de revue ne se défait pas silencieusement). Upload (`documents/received-invoices/`, endpoint dédié, PAS le contrôleur générique) : stockage content-addressed (`storage.ts`, imite `archive/storage.ts`, `DOCUMENTS_INBOUND_DIR`), hash SHA-256, refus nommé du doublon ; extraction structurelle CII/UBL/Factur-X (`extraction.ts`, régex namespace-agnostique reprise du repère — jamais `fromXml`, le bug de round-trip CII documenté en mémoire de session ; Factur-X via `pdf-lib`/`decodePDFRawStream`, même technique que `facturx-provider.spec.ts`) qui PRÉ-REMPLIT un brouillon, jamais un document déjà approuvé ; extraction impossible → champs vides, fichier quand même attaché, jamais un refus. Écran : bouton « Upload » (nouveau slot `list-header-extra`, additif), bouton de téléchargement (`list-row-extra`), tout le reste (liste, formulaire, actions) entièrement générique. Règles pays FR/US pour receive/approve/reject/delete — FR cite la RÉCEPTION obligatoire par plateforme agréée dès 2026-09-01 (economie.gouv.fr/impots.gouv.fr, consultés 2026-08-27, repris du repère) comme fait à part, distinct de ce workflow interne : cet écran aide à la bookkeeping, il ne satisfait PAS à lui seul l'obligation légale (canal agréé non branché — voir TODO_ISSUES.md). Hors périmètre, consigné : boîtes de réception par canal (KSeF/PDP/Peppol entrant, remainder de l'item 10), rapprochement fournisseur, OCR. Prouvé par l'écran (spec 36) + régression 11/17. |

## E — Données pays

| # | Titre | Description |
|---|---|---|
| 19 ✅ | **Allemagne + Royaume-Uni** ⚖ (fait, 2026-09-01) | Les sources primaires LUES en ligne : UK VAT Regulations 1995 reg. 14(1)(d) — première entrée `legal` du catalogue d'identifiants — et § 14/14a UStG cités verbatim (gesetze-im-internet.de, legislation.gov.uk). Deux refus honnêtes de surinterpréter : § 37a HGB couvre les « Geschäftsbriefe » (pas « Rechnung ») et SI 2015/17 reg. 25 les « business letters » (pas les factures) — les LEGAL_ID restent unverified. Pas de pattern UK faute de source sur le format, en le disant. Les 5 rouges de 05-clients éteints (23/23) : la base de rouges permanents passe de 6 à UN (item 20). |
| 20 | **Couverture recherche 40→118** | La liste des pays de `company-lookup` venait des profils supprimés. Décider de sa nouvelle source. |
| 21 ✅ | **Sourcer FR et US** ⚖ (fait, 2026-09-01) | Textes lus en direct sur codes.droit.org (miroir Légifrance — Légifrance lui-même a continué de refuser tout accès automatisé) et govinfo.gov (US Code officiel). Promus `legal` : les 5 taux de TVA FR (CGI art. 278/278 bis+279/278-0 bis/281 quater+281 octies+298 septies/293 B — deux « divergences » de sources secondaires se sont révélées être des articles PARALLÈLES, pas contradictoires), le VAT scheme FR des identifiants (242 nonies A), et 3 règles country-policy FR (quote.send : code civil art. 1366 ; invoice.send : CGI art. 289 VI, pas V ; invoice.save-draft : CGI art. 289 I.5). E-SIGN Act (US) recontrôlé sur govinfo.gov. TROUVAILLE À DÉCISION PRODUIT, pas corrigée en douce : R.123-237 (C. com.) et 242 nonies A (CGI) exigent tous deux le SIREN sur une facture, jamais le SIRET — le champ LEGAL_ID reste `unverified`+SIRET/14 chiffres inchangé, la divergence est consignée dans TODO_ISSUES.md. Restent honnêtement non sourcés : les taux de sales-tax US par État (une pile de dispositions par État, pas un seul texte — vérifié sur CA, § 6051 ≠ le taux composite encodé) et la plupart des règles « commodité produit » FR/US (aucun texte à trouver). |

## F — Infrastructure

| # | Titre | Description |
|---|---|---|
| 22 ✅ | **Mode worker & files d'attente** (fait, 2026-08-31) | BullMQ recâblé (split Core/Module/Worker, `WORKER_INLINE`, `jobId` déterministe, Redis requis au boot) sur le modèle du repère. Job générique `(companyId, typeId, documentId, actionId, payload)` rejoué par `DocumentsService.runAction` — les quatre portes y compris côté worker. "send" devient asynchrone pour quote/invoice/credit-note : `draft/send_failed → sending → sent \| send_failed`, numéro pris en entrant dans `sending`. Item 5 (relances) réutilisera le même job. |
| 23 ✅ | **Serveur MCP** (fait, 2026-09-01) | Factory stateless reprise du repère (un McpServer par requête HTTP, clé API + scopes ; un outil hors scope n'apparaît PAS dans tools/list plutôt que d'échouer à l'appel). Les outils sont devenus GÉNÉRIQUES par descripteur : list_document_types (ce que le pays autorise, champs et actions déclarés — le LLM lit le descripteur), list/get_document, get_document_pdf_link (via share-links, item 24), et run_document_action — qui traverse DocumentsService.runAction : LES QUATRE PORTES valent au MCP par construction, leurs messages nommés remontent tels quels au LLM. Scopes en deux temps (coarse à l'enregistrement, précis par typeId à l'appel, échec fermé sur scope inconnu) ; credit-notes/expenses/received-invoices ajoutés aux scopes. Prouvé par un VRAI fil HTTP (initialize → tools/list → tools/call, save-draft réel, refus de scope réel), gaté MCP_HTTP_LIVE=1. |
| 24 ✅ | **Liens publics de téléchargement** (fait, 2026-09-01) | `DocumentDownloadToken` : token 256 bits renvoyé UNE fois, seul le hash SHA-256 stocké (recherche exacte par index, jamais du password-hashing — le repère l'expliquait déjà), expiration 30 jours (l'1h du repère était un lien jetable de chat MCP — l'écart argumenté dans share-links.service.ts), révocation par revokedAt. Les trois refus (inconnu/expiré/révoqué) sont LE MÊME 404 — jamais un oracle. Action `share-link` déclarée sur quote/invoice/credit-note (les quatre portes gratuites, refus nommé sur brouillon), servie en REST comme download-xml. GET public @Public() qui sert LE MÊME renderInstancePdf que l'authentifié, signature comprise. Écran : dialogue de partage + liste des liens actifs + révocation. Spec 37 : visite SANS session → 200 PDF, révoqué → 404. |
| 25 ◐ | **Contributions manquantes** (contributions faites 2026-08-31 ; reste l'i18n des libellés) | Le dashboard n'a que la facture. « Les dépenses du mois », devis, avoir ; et l'i18n des libellés de descripteurs (données brutes aujourd'hui). |
