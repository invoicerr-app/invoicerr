# TODO_FEATURES — remaining product work

> This file tracks only what remains. Every shipped feature, every product decision, and the full
> history live in git (`git log`) — last commit before this rewrite: `ad6d0bd3` (2026-09-15).

## Remaining

| Item | State | e2e to prove |
|---|---|---|
| **UI** — full UI/UX redesign with the `better-ui`, `emil-design-eng`, `tailwind-design-system`, `better-typography`, `accessibility`, `om-ux-review-pr` and `playwright-screenshot-inspector` skills | Owner decision, 2026-09-15 evening: a strong, unique visual identity — an accent colour and palette expressed through the shadcn CSS variables (Tailwind v4 `@theme inline`), NOT plain black-and-white, NOT orange/amber, NOT green; then every screen, component by component. Shipped: identity « Lagune » tokens light/dark + fonts (6e604b2f), lighter destructive red (63891c1d), screen 1 document page + short create dialog (9dfb5b92, validated), screen 2 document list + empty states (ef7b1e0b, validated), screens 3-6 dashboard/statistics, clients/articles/time/bank/declarations, settings + account, auth/onboarding/portal/signature (654fc86e), transversal pass — tokens everywhere, one primary per screen, `aria-label` on icon-only buttons, `<html lang>`, mobile density of the document page, public signature route outside the app shell, onboarding on mobile (9b4474d0). Screens 3-7 validated and deployed (image 90225640); checkboxes follow the theme (5467eedc); big dialogs are step wizards — document creation, client, bank import, article, time entry (db79a281, 1e108cd8). Remaining: the chosen logo (series 3, owner picks on 2026-09-16) to derive into favicon / PWA icons / apple-touch-icon / auth brand mark (`data-slot="brand-mark"`); form sections by title in the document form would need a grouping field on the backend descriptor. | None — the owner validates from screenshots and GIFs. |
| **A** — regional payment providers (Payplug FR, Przelewy24 PL, Nexi IT, Easypay/IfThenPay PT) | Deferred by owner decision — only if clients in those countries specifically ask. Stripe, Mollie and PayPal are shipped and proven (a real captured payment for each of the three); the four regionals are not started. | Live specs gated on each provider's own env flag, once wired — mirroring `stripe.live.spec.ts`/`mollie.live.spec.ts`/`paypal.live.spec.ts`. |
| **E** — hosted offering: what remains | Hosting provider still to choose (`[HOSTING PROVIDER, COUNTRY]` placeholder in the Terms of Service); zip/deletion lifecycle never executed outside jest; the "customer stops paying" webhook cycle never observed live; Polar production organisation/products/endpoint not created (only the sandbox is proven); DMARC hardening to `p=quarantine` once reports are clean. | A real "stops paying" cycle observed on the running app (blocked → zip → deletion), and `POLAR_LIVE=1` confirming the production organisation's products. |
| **Clean-up** — the big clean-up before/after the PR | `git stash@{0}` dropped (2026-09-16); what remains is the stale scratch files and the items still listed below. | None — a housekeeping pass; verified by a clean `git status`. |

### From the former TODO_ISSUES.md

`TODO_ISSUES.md` (the technical logbook) is deleted — see git history. Every item below was still
open when it was folded into this table on 2026-09-15 (four items closed during the day — Chorus Pro `IN_*` status mapping, the worker-replay spec, the CI run, the `14-articles` flake — were dropped); state is carried over, translated to English
where the source was French.

| Item | State |
|---|---|
| Chorus Pro — minor unresolved notes | Found along the way, lower stakes: the exact legal basis for Chorus Pro refusing cheque/cash/Stripe as a payment method (formal prohibition vs. no observed usage); whether a post-2020 AIFE document added B1/S1/M1 as alternative billing frameworks to A1-A25; the Portuguese NIF check-digit algorithm is not sourced anywhere in this repo (already flagged as such in `country-identifiers/data/pt.json`). |
| French inbound e-invoicing obligation not satisfied | Receiving e-invoices has been mandatory in France since 2026-09-01. The manual received-invoice upload screen does not, by itself, satisfy it — it requires an accredited platform. No accredited inbound channel is wired up (inbound KSeF, inbound PDP/Peppol deposit) — the named remainder of the conformity poller on the receiving side. |
| Legacy-install upgrade never proven | Owner go-ahead already given (2026-09-13); what's missing is someone actually replaying `sync-schema.ts` with `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION` against a throwaway database. |
| Italian recipient code has no screen | `country-identifiers/data/it.json` only declares VAT and LEGAL_ID for an ordinary Italian B2B client — `IT_SDI`/PEC have no input field, so a domestic Italian B2B invoice goes out with the `codiceDestinatario = 'XXXXXXX'` fallback. The backend mechanism exists; only the screen is missing, the same shape as the VAT identifier gap already fixed. |
| Portuguese B2G transport not determined | The channel is delegated to a portaria (CCP art. 299.º-B n.º 5) never located in its raw text; one attempt returned an HTML page instead of the document. `transportId` remains unwritten, so `b2g-routing/data/pt.json` can't be honest yet. |
| `reporting/` declarative catalog only covers Portugal | France's CGI art. 290 obligation is established but discharged through the PDP (already implemented and proven live as a transport); the schema can only express a `providerId`, never "discharged by an already-implemented transport". Needs determining what the PDP forwards automatically vs. what stays the product's responsibility (B2C, foreign clients) before writing `fr.json`. |
| Polish FA(3) KOR pattern not reimplemented | The old compliance engine had a full KOR (post-clearance correction) mode; the current `invoice.descriptor.ts` has no correction link compatible with that shape — a Polish corrective invoice goes out today as an ordinary FA(3) invoice, never as a KOR. |
| French comments in the code | A sweep last year measured 310 files with a French comment; the catalogue JSON prose was translated on 2026-09-15 — the `.ts` comments remain to be re-measured and translated. |
| PT `pt-at` — mTLS not wired | The AT requires a signed X.509 client certificate for the HTTPS connection itself; `pt-at-client.ts` uses plain `fetch()`. Planned in the same `pfx`/`passphrase` shape as `transports/sdi/sdicoop-client.ts`, deliberately not done blind without a real PKCS#12 to test against. |
| PT `pt-at` — nonce RSA padding unverified | The AT manual names "RSA" with no padding parameter; PKCS#1 v1.5 was chosen as the best reading (OAEP ruled out as Node's default), to be confirmed on the first real round-trip (a wrong padding fails loudly: `CodigoResposta` 16/17). |
| KSeF status poller never proven live | `KSEF_AUTH_TOKEN` is absent from this environment, so two points remain unverified: (a) the `{code, description, details}` → terminal/rejected mapping is extrapolated from `authenticate()`'s own convention, never confirmed for the `invoiceStatus` endpoint itself; (b) whether `invoiceStatus` still responds once `send()` has closed the session is unknown to date. Distinct from the KSeF SEND channel, already proven live (2026-06-28). |
| KSeF PROD key missing | Only a vendored MF key for the TEST environment exists; no PROD key has ever existed in this repo, so `loadVendorizedKeys('prod')` fails loudly by design rather than falling back to the test key against a real production KSeF. Not done for lack of a real PROD company to date. |
| B2G: 13 EU member states covered by no rule | `b2g-routing/data/` covers 15 countries; the 13 missing ones break down into a national CIUS never vendored (AT, DK, FI, HR, PT CIUS-PT, RO, SI, SK) or a closed channel with no confirmed Peppol reachability (BG, CZ, HU). The honest refusal ("no B2G rule declared") is the intended behavior as long as the CIUS isn't vendored. |
| B2G: 21 covered countries sourced only from a Commission fact sheet | For 21 of the countries already covered, the only source read is the Commission's eInvoicing fact sheet (DG CNECT) — a report, not the national transposition text — to be cross-checked country by country someday, the same provenance work as the French SIREN/SIRET. |
| Cross-border seller×buyer composition — one layer read out of two | `correction-routes/`/`cancel-policy.ts` only read the SELLER country (which document the country imposes), never the "taxable base" layer (attached to the TAXATION country, potentially the buyer under reverse charge). The composition of the two is not written — needed if a cross-border correction must one day distinguish "which document" from "how and until when the taxable base can be reduced". |
| `ClientsModule` cannot be imported under ts-jest | The chain `ClientsModule → WebhooksModule → drivers/discord.driver.ts → @teever/ez-hook` (a pure-ESM JSR package) doesn't compile under ts-jest. Any future test that imports `ClientsModule` as a module (not just `ClientsService` in type position) will rediscover it. To decide: ts-jest ESM config, or replace the Discord driver's dependency. |
| Mentions — per-company customization not done | All three FR mentions are `statutory: true` (statutory default rate, €40 indemnity, doctrinal early-payment-discount wording); nothing lets a company stipulate a different value. No need expressed to date; the interpolation mechanism is already ready to accept a third value source the day this need arises. |
| No regional WORM/S3 archive provider | Only one provider exists (local content-hash-addressed persistence). Would only be reopened with a real data-residency need (MX/BR/SA) and real AWS credentials — neither exists in this environment. |
