# TODO_FEATURES — remaining product work

> This file tracks only what remains. Every shipped feature, every product decision, and the full
> history live in git (`git log`) — last commit before this rewrite: `ad6d0bd3` (2026-09-15).

## Remaining

| Item | State | e2e to prove |
|---|---|---|
| **Legal** — SaaS legal set | Shipped 2026-09-16 (ff2b5d4d, 3ade7e5f) and rewritten on 2026-09-17 (version 2026-09-17, re-acceptance triggered) with the `legal-tos-privacy`, `terms-of-service`, `dpa-drafting` and `gdpr-compliance` skills: account registration, acceptable use, data ownership and portability, indemnification, force majeure, liability carve-outs, no DPO / no automated decisions, Art. 28(3)-(4) controller rights and sub-processor cascade. The hosting-provider placeholder is **resolved** (2026-09-19): every document names Scaleway SAS, Paris, for the cluster, the object storage and — since the same day — the managed database, which replaced a US-parented provider in Frankfurt. The residency statements were strengthened accordingly without overclaiming: the sub-processors that may still transfer outside the union for payment, e-mail and content delivery are listed unchanged. Translated into all six languages. **Remaining: lawyer review of the drafts** (arbitration and one-year limitation clauses deliberately left out pending advice). | `75-legal-acceptance.cy.ts` (green in both modes). |
| **Seats** — purchased seats and the open-space page | Shipped 2026-09-16 (validated on screenshots after nine passes): seat quantity is read from Polar only (the app never writes it — a static spec guards against it), one seat per member, empty seats allowed, no break room, invitation refused without a free seat (`NO_FREE_SEAT`), over-capacity parks the most recent members (never the OWNER) on a waiting screen. Settings > Seats: top-view open space in inline SVG (desk pairs back to back, seeded desk props, chairs with avatars, bin on the aisle side, lamp or plant between two rows), drag-and-drop with exact pointer tracking, list view on mobile. | `76-seats.cy.ts` (green). |
| **E** — hosted offering: what remains | Hosting provider **chosen** (Scaleway, Paris — see the Legal row). Remaining: the Polar production organisation, its products and its endpoint are not created, only the sandbox is proven; DMARC hardening to `p=quarantine` once reports are clean. Proven on 2026-09-16 against a throwaway database: the full "customer stops paying" cycle — signed Polar webhook ACTIVE→PAST_DUE, real sweep PAST_DUE→BLOCKED, J-7/J-1 mails in Mailpit, real zip (PDF + JSON), deletion refused while a Polar subscription still exists, cascade deletion for a never-paid company. | `POLAR_LIVE=1` confirming the production organisation's products. |
| **Kubernetes** — production deployment that scales | Owner decision, 2026-09-17, **amended 2026-09-19**: Scaleway Kapsule (Paris) for the cluster, Scaleway Object Storage for archived documents, and — replacing Neon — **Scaleway's own managed PostgreSQL, in Paris**, so the whole chain sits with one provider in one country and the database is reached over the private network rather than the public internet. Shipped tonight: S3-compatible archive provider (proven against MinIO), Helm chart `deploy/helm/invoicerr` (api, worker, Redis, OCR, optional Postgres, Ingress + cert-manager, init containers) proven on a kind cluster with a real invoice sent and its PDF found in the bucket, `kubernetes.md` guide. Shipped 2026-09-19, and this is what made the topology possible at all: a single read-write-once volume mounted by both the API and the worker forced them onto one machine, because the inbound store for uploaded files had no object-storage mode. It has one now, and the volume disappears from the chart when both stores are on object storage. An analysis of 203 route handlers then found **seven** things that would have broken at three API replicas and fifteen workers — the client address collapsing behind a second proxy hop, rate-limit counters and the single-sign-on registry living in process memory, the legal-release e-mail sending once per replica, two queues unreachable from the workers, and a retried send that could deliver an invoice or a tax filing **twice** because its guard only held inside one process. All seven are fixed and proven. Backups are now encrypted before they leave the process, with a restore proven by an independent client holding only the key. Remaining, owner side: finish creating the cluster (mutualized control plane, one ARM node, ~73 €/month), the Object Storage bucket, the managed database, DNS for `invoicerr.app`, real secrets; then the first real deploy. | A real deploy of the chart on Kapsule, `/api/health` and one invoice sent through it. |

### From the former TODO_ISSUES.md

`TODO_ISSUES.md` (the technical logbook) is deleted — see git history. On 2026-09-16 the owner decided to drop from this file every line deferred by decision (regional payment providers, S3/WORM archive, per-company mentions, cross-border composition, B2G coverage beyond the five in-scope countries, controller-module tests under ts-jest); they live in git history. What remains below is blocked on credentials or on an external party.

| Item | State |
|---|---|
| French inbound e-invoicing — PDP reception **proven**, two production-only checks left | Reception through the PDP is wired and proven in the sandbox (self-addressed deposit → inbound twin listed, downloaded, created as a received invoice, approved, settled — commit 25af6a6d). Nothing is left to build. Two points cannot be settled without a production platform: the buyer lifecycle status endpoint answers a generic 404 on the free sandbox (kept non-fatal, to re-verify on a production PA), and the exact numeric code of a buyer refusal is a best-effort choice, not sourced. |
| `reporting/` — French e-reporting calendar unread | `fr.json` shipped on 2026-09-16 with a `dischargedBy` + scope axis: domestic B2B is discharged by the PDP transport (CGI art. 289 E, raw text); B2C, international and payment data are the company's own duty, kept `unverified` because the calendar (decree 2022-1299 as amended, 2026-09-01 / 2027-09-01 by company size) could not be read raw from this machine (Légifrance and BOFiP blocked). To close: read the decree raw and flip the two facts to `legal`. Re-probed on 2026-09-19 and still blocked, but the blockage is now precise rather than general: Légifrance answers 403 to a plain request on both of the decree's routes, while the tax administration's own doctrine site answers 200 and serves only a JavaScript shell carrying none of the text. A viable route exists and needs no new account — the PISTE credentials already obtained for the French B2G channel also gate an official Légifrance API, which returns article text as data; it requires subscribing an application to that API. |
| ~~PT `pt-at` — mTLS not wired~~ | **Done 2026-09-19.** The AT requires a signed X.509 client certificate on the HTTPS connection itself; the client now uses `node:https` with native `pfx`/`passphrase`, the same shape as `transports/sdi/sdicoop-client.ts`, and the certificate material rides in the existing encrypted channel-config blob (no schema change). The earlier reasoning — "not doable blind without a real PKCS#12" — held only for half the problem: a self-signed archive generated at test time, against a local server demanding a client certificate, proves the wiring end to end. Three tests do exactly that: the certificate reaches the handshake and the server sees it, a missing certificate is refused at the TLS layer with a named cause, and a wrong passphrase throws synchronously at agent construction. Writing them surfaced a real defect: Node's pooled global agent let a wrong-passphrase request ride an earlier request's already-authenticated socket, so the check silently passed; connection reuse is now disabled on this path. What no test here can establish is whether the authority's own PKI accepts our production certificate. |
| PT `pt-at` — nonce RSA padding unverified | The AT manual names "RSA" with no padding parameter; PKCS#1 v1.5 was chosen as the best reading (OAEP ruled out as Node's default), to be confirmed on the first real round-trip (a wrong padding fails loudly: `CodigoResposta` 16/17). |
| KSeF status poller never proven live | `KSEF_AUTH_TOKEN` is absent from this environment, so two points remain unverified: (a) the `{code, description, details}` → terminal/rejected mapping is extrapolated from `authenticate()`'s own convention, never confirmed for the `invoiceStatus` endpoint itself; (b) whether `invoiceStatus` still responds once `send()` has closed the session is unknown to date. Distinct from the KSeF SEND channel, already proven live (2026-06-28). |
| KSeF PROD key missing | Only a vendored MF key for the TEST environment exists; no PROD key has ever existed in this repo, so `loadVendorizedKeys('prod')` fails loudly by design rather than falling back to the test key against a real production KSeF. Not done for lack of a real PROD company to date. |
