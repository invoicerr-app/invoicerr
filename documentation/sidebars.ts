import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

/**
 * One sidebar per top-level navbar section, so navigating to a section (User Guide, Developer
 * Guide, ...) only shows that section's pages, instead of the whole docs tree. Getting-started
 * content (introduction, installation) lives inside the User Guide sidebar (`docs/user-guide/`)
 * rather than as its own section — four sections total, matching the navbar: User Guide,
 * Developer Guide, Legal, and Compliance (the latter is not a `docs`-plugin sidebar at all: its
 * per-country pages are generated by `plugins/compliance-content-plugin.ts` from
 * `compliance/<CC>-<Country>.md` and listed on the `/compliance` index page instead).
 */
const sidebars: SidebarsConfig = {
  developerGuideSidebar: [
    {type: 'autogenerated', dirName: 'developer-guide'},
  ],
  userGuideSidebar: [
    {type: 'autogenerated', dirName: 'user-guide'},
  ],
  // Terms of Service, Privacy Policy, Data Processing Agreement, Refund and Cancellation Policy,
  // Legal Notice, Cookies & Acceptable Use, International Access Transparency — `docs/legal/`,
  // ordered by each file's own `sidebar_position` front matter (1-7, see each file). Added
  // 2026-09-16 alongside the four new documents and the in-app acceptance flow
  // (`backend/src/legal/`, `frontend/src/pages/legal/`) — before this, `terms-of-service.md` built
  // as a page but was reachable only by direct URL, in no sidebar and no navbar entry.
  // International Access Transparency joined 2026-09-19 (EU Data Act, Article 28) as a sixth
  // reference document, same pattern as the other three non-gated ones. The Refund and Cancellation
  // Policy joined 2026-09-20 as a seventh, and took position 4 — it reads with the commercial
  // clauses, not with the reference material — which pushed the last three down one each. That
  // renumbering is free: `sidebar_position` lives in front matter, which is stripped before a
  // document's content hash is computed, so moving a document in the list never re-prompts a user.
  legalSidebar: [
    {type: 'autogenerated', dirName: 'legal'},
  ],
};

export default sidebars;
