  name: invoiceerr-backend-dev

  services:
    database-postgresql:
      image: postgres:15
      restart: always
      environment:
        POSTGRES_DB: ${POSTGRES_DB}
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ports:
        - '5431:5432'
      volumes:
        - docker-postgres:/var/lib/postgresql/data


  volumes:
    docker-postgres: